var React = require('react');

var Title = React.createClass({
    render: function() {
        return (
            <div className="title">
                PumpUp Coop Challenge
            </div>
        );
    }
});

module.exports = Title;